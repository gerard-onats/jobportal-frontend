// Playground component - Components are created/styled here in an isolated manner, and then later placed at the correct component folder.

const Playground = () => {
    return (
        <></>
    );
}

export default Playground;